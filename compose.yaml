services:
  blog:
    build:
      context: .
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-bookworm-slim}
    restart: unless-stopped
    env_file:
      - .env.node.local
    ports:
      - "127.0.0.1:3100:3100"
    volumes:
      - ./data:/app/data
